name: fit_assist
description: A new Flutter project for FitAssist.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.10.2
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.0.0
  cupertino_icons: ^1.0.2
  lottie: ^3.1.2
  fl_chart: ^0.69.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
